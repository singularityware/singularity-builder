language: cpp

matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required

compiler:
  - gcc
  
install:
- ./singularity_build.sh setup build

script:
- ./singularity_build.sh install
